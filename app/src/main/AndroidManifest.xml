<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ebjacson.lanchonetepdv">

    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <meta-data
            android:name="DATABASE"
            android:value="lanchepdv.db" />
        <meta-data
            android:name="VERSION"
            android:value="2" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="com.example.ebjacson.lanchonetepdv" />

        <activity android:name=".view.impl.MapaMesasActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.impl.VendaActivity"
            android:label="@string/venda"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.impl.VendaConfirmActivity"
            android:label="@string/confirmvenda"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.impl.VendaDetalhesActivity"
            android:label="@string/detalhesvenda"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>